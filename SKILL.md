---
name: travel-itinerary
description: 智能旅行攻略生成，按时间线整合景点、导航、餐饮、住宿，支持HTML交互式地图和服务器部署，可集成flight-compare进行实时价格查询
version: 3
author: chenkunqing
tags: [travel, itinerary, planning, chinese, visualization, deployment, html-map, price-query, flight-compare]
---

# 旅行攻略文档生成

## 触发条件

用户明确要求制作旅行攻略/行程规划**并提供基本信息**时使用。

**相关触发场景：**
- 用户要求制作旅行攻略/行程规划
- 用户询问旅行预算或费用
- 用户需要机票、酒店、火车票价格信息（可与flight-compare技能集成）
- 用户要求路线优化或行程审查

**不算触发的情况：**
- 用户只是随便问问"去哪里好玩"
- 用户只是查询某个景点信息
- 用户要求的是景点清单而非行程
- 用户只查询机票价格而不需要行程规划

## 启动流程（必须执行）

### 第1步：信息收集（未收集足够信息前不开始规划）

必须向用户确认以下信息：

| 信息 | 必要性 | 典型问法 |
|------|--------|----------|
| 目的地 | 必填 | "去哪个城市/地区？" |
| 天数 | 必填 | "玩几天？" |
| 人数/人群 | 必填 | "几个人？有没有老人小孩？" |
| 出行方式 | 必填 | "自驾还是公共交通？" |
| 预算 | 建议 | "人均预算大概多少？" |
| 偏好类型 | 建议 | "喜欢自然风光还是人文历史？" |

**注意：** 用户说"去西藏玩5天"，不能直接开始写，必须先确认以上信息。

### 第2步：确认大框架

在细化之前，先输出行程大纲：
- 每天的住宿点（城市/县城）
- 主要景点
- 预计车程

让用户确认方向正确后再细化。

### 第3步：价格查询（可选，与flight-compare技能集成）

当用户需要价格信息时，使用flight-compare技能查询实时价格：

#### 机票价格查询
```bash
# 往返机票查询
node /home/ubuntu/flight-compare/scripts/compare.js --from "出发城市" --to "目的城市" --depart "YYYY-MM-DD" --return "YYYY-MM-DD"

# 单程机票查询
node /home/ubuntu/flight-compare/scripts/compare.js --from "出发城市" --to "目的城市" --depart "YYYY-MM-DD"
```

#### 酒店价格查询
```bash
node /home/ubuntu/flight-compare/scripts/flyai_hotel.js --dest "城市名" --check-in "YYYY-MM-DD" --check-out "YYYY-MM-DD"
```

#### 火车票查询
```bash
node /home/ubuntu/flight-compare/scripts/flyai_train.js --from "出发城市" --to "目的城市" --depart "YYYY-MM-DD"
```

**注意**：价格查询结果仅供参考，机票和酒店价格实时变动。详见 `references/price-query.md`。

## 工作流程（分阶段交付）

1. **大框架阶段**：先出每天去哪、大致路线，让用户确认方向
2. **方案对比阶段**：如果用户有参考方案，列出优缺点对比表让用户选择
3. **调节阶段**：根据用户反馈增删景点、调整节奏（每天车程、出发时间等）
4. **汇总变更**：每次调整后，明确告知"砍掉了什么/保留了什么/为什么"
5. **最终版阶段**：输出完整时间线文档（一次性交付完整版，不要中途频繁汇报）

关键交互细节：
- 用户可能发来参考攻略（图片/文字），要认真对比分析优缺点
- 用户可能逐步提出约束（人员年龄、体力、偏好、预算），每次都要重新评估可行性
- 出发时间要根据当地实际作息反复确认（用户可能觉得某个时间太早/太晚）
- 某个景点值不值得去，要给出"性价比分析"（如等4.5h看日落+第二天要早起=不合理）

## 核心原则（按重要性排序）

1. **不割裂**：每个景点的所有信息（介绍、门票、怎么玩、注意事项）都写在对应的时间段内，不要单独拆出来做表格或汇总章节。
2. **时间线驱动**：以"几点到几点做什么"为主轴，所有信息嵌入时间线。
3. **导航明确**：每个地点都写清楚"📍导航搜什么关键词"，告诉用户在高德/百度地图里搜什么。
4. **景点要解释**：不能只写景点名，必须用1-3句通俗大白话解释"它是什么/为什么值得去/长什么样"，让没去过的人脑海中能浮现画面。
5. **实用性优先**：门票怎么买、能不能开车进去、要不要提前预约、穿什么衣服——这些执行层面的信息比抒情描写重要得多。

## 文档结构

参考模板：`templates/day-entry-example.md`（展示了一个理想的Day条目写法）

```markdown
# 标题（目的地+天数+关键词）

> 人数、出行方式、预算、核心偏好
> 更新日期

---

## 第X阶段：阶段名称（租还车地点/交通方式）

### Day N（日期 星期）：起点 → 终点（开车约Xh）

- **HH:MM - HH:MM**：【类型】具体安排
  - 📍导航："搜索关键词"
  - 门票：价格/免费。是否需要预约。能否自驾进入。
  - **它是什么**：用1-3句大白话解释这个地方是什么、为什么出名。
  - **怎么玩**：具体步骤，先去哪再去哪，玩多久。
  - **注意**：该地点特有的坑/提醒。
  - ⏰ 营业时间（如适用）

- **住宿**：搜索关键词，价格区间，特别提醒。
  - 联系方式/预订方式
```

## 每个时间段必须包含的信息

### 景点类（内容要丰富！用户明确要求"更多更丰厚一点的景点攻略"）
1. 📍 **导航关键词**（高德地图搜什么）
2. 🎫 **门票**：价格/免费 + 是否需预约 + 能否自驾进入（如不能要说坐什么车）
3. 🏷️ **它是什么**：1-3句通俗描述+历史/背景/特色，让人能想象出画面（如"像外星球""中国最美公路"），可以写得更丰富一些
4. 🎮 **怎么玩**：按步骤写（先→再→最后），标注每步大约时间，核心看点要突出
5. ⏰ **营业时间**：开门时间、停止入园时间
6. ⚠️ **注意事项**：该景点特有的（如海拔、温度、末班车时间、不能停车等）
7. 💡 **省钱tip**：哪里可以免费看类似景色、公路旁是否有免费替代点

### 内容丰富度示例（用户期望的详细程度）
```
门票70元/人+自驾环湖100-150元/车 · 可以开车进去自驾环湖70公里！ · 新疆海拔最高(2073米)面积最大的高山湖泊，被叫做"大西洋的最后一滴眼泪"——大西洋水汽的最东端 · 湖水蓝得不真实，6月湖边开满黄色紫色野花背景是雪山360度无死角 · <strong>玩法(3-4小时)：</strong>建议逆时针方向(先北岸下午光线从南照过来拍北岸最好看)→看到漂亮地方随时停车→必停：西海草原(湖边最大草地野花区拍照最佳)→天鹅乐水区域(碰运气看天鹅)→日落约21:30在西岸最美太阳落入雪山湖面 · <strong>注意：</strong>湖边风非常大气温约10-15°C必须穿外套甚至冲锋衣！
```

### 为HTML地图优化的格式

当攻略需要用于HTML移动端展示时，内容应该：
- 用`<strong>`加粗景点名
- 用`<span style="font-size:12px;color:#666;">`包裹详细信息
- 用`·`分隔不同信息点
- 避免长段落，多用箭头`→`连接步骤

```html
<div class="timeline-content">
    ⭐ <strong>景点名</strong>描述<br>
    <span style="font-size:12px;color:#666;">门票信息 · 它是什么描述 · <strong>玩法：</strong>步骤1→步骤2→步骤3 · <strong>注意：</strong>注意事项</span>
</div>
```

### 餐饮类
1. 📍 导航/位置（如"县城主街""夜市"）
2. 推荐菜品 + 价格
3. 人均消费
4. ⏰ 营业时间
5. 💡 特色推荐理由

### 住宿类
1. 📍 搜索关键词
2. 价格区间（按间计价）
3. 联系方式/预订方式
4. ⚠️ 特别提醒（如需确认送机、凌晨入住、高原不洗澡等）

### 交通/赶路类
1. 导航目的地
2. 走哪条路（高速编号/国道名）
3. 路上有什么可看的（如果有）
4. 到达前需要做的事（如加油、买干粮）
5. ⚠️ 应急备选方案（如拥堵/封路绕行建议）

## 文档末尾必须附带

1. **💰 费用汇总表**：分项列出（租车、油费、住宿、门票、餐饮、购物），给出总计和人均。可集成flight-compare技能获取实时价格（机票、酒店、火车票）
   ```markdown
   | 项目 | 预估费用 | 实时价格 | 备注 |
   |------|----------|----------|------|
   | 机票（往返） | ¥4000 | ¥3650 | 携程+飞猪对比最低价 |
   | 住宿（5晚） | ¥2500 | ¥2800 | 4星级酒店 |
   | 餐饮 | ¥1000 | ¥950 | 每天约¥200 |
   | 门票 | ¥500 | ¥480 | 景点门票 |
   | 交通 | ¥300 | ¥280 | 当地交通 |
   | **总计** | **¥8300** | **¥8160** | |
   ```
2. **✅ 出发前必做清单**：分类（证件、预订、手机准备、物品清单）
3. **🔑 当地特别规则**：该目的地独有的规则/常识（如新疆加油规则、时差等）
4. **🌧️ 应急方案**：天气不好/景点关闭时的备选方案
5. **💡 价格查询说明**：注明价格数据来源和时间，提醒价格实时变动

## 规划过程中的交互原则

1. **先出大框架**让用户确认方向，再细化
2. **有多种方案时**列出优缺点对比，让用户选
3. **发现时间冲突/赶路问题**主动提出并给解决方案
4. **考虑人员组成**：年龄、体力、偏好影响景点选择和节奏
5. **住宿省钱策略**：住附近县城比景区便宜很多，主动推荐
6. **每天出发时间统一**：根据当地作息确定（如新疆10:00出发），保持节奏一致
7. **预算对齐**：根据用户预算选择合适档次的住宿和餐饮

## 格式要求

- 用 markdown 格式
- 时间用粗体 `**HH:MM - HH:MM**`
- 类型用方括号 `【景点】【取车】【午餐】【入住】`
- 导航用 📍 emoji 标识
- 景点评分用 ⭐ 表示（1-5颗）
- 重要提醒用 ⚠️ 标识
- 省钱技巧用 💡 标识
- 营业时间用 ⏰ 标识

## 注意事项

- 不要把景点信息拆到单独章节，必须嵌入时间线（用户明确说"割裂"是大忌）
- 每天车程时间要明确标注在标题括号里
- 如果某天车程超过6h，要想办法拆分或标注为"辛苦日"并给出缓解方案
- 时间安排要留buffer，不要卡得太紧（特别是赶飞机/还车的日子）
- 长辈/小孩同行要降低每日强度
- 出发时间考虑当地实际作息（如新疆晚2小时，不要按东部时间安排）
- 每天结束时间也要合理（晚上23:00才回酒店+第二天早起=不合理）
- 两个景点体验类似时建议砍一个换成不同类型的（如已有顶级草原A，不需要再去类似的草原B）
- 信息收集不全时不要开始写规划

## 输出模式

用户可能只需要不同详细程度的行程，可根据需求调整：

- **完整版**：含时间线、门票、导航、住宿、费用汇总
- **精华版**：只列每天主要景点和住宿点
- **清单版**：纯景点+餐饮推荐列表，无时间线

---

## 路线审查（用户要求检查行程时）

当用户问"有没有走重复路"或"检查行程问题"时，按以下维度审查：

1. **重复路段检测**：标出所有原路返回的路段，区分"不可避免的死胡同路（如塔县G314）"和"可优化的回头路（如精河↔赛里木湖）"
2. **赶时间风险**：标出当天有硬性截止时间（航班/还车）的日子，检查buffer是否够（建议至少留1.5h）
3. **睡眠不足风险**：前一天晚到（>23:00入住）+第二天需长途驾驶=危险组合
4. **住宿可订性**：偏远小镇旺季住宿选择少，主动提醒提前预订+给备选方案
5. **不确定因素**：季节性道路（如独库公路开通时间）需准备Plan B
6. **加油/充电盲区**：连续100km+无加油站的路段要明确标注"出发前加满"

审查结果格式：按严重程度排序，⚠️⚠️最紧急 → ⚠️需注意 → 💡优化建议。每个问题给出具体解决方案，不要只提问题不给答案。

## 路线图创建（用户要求路线图时）

当用户已经有详细攻略但需要"路线图"或"每天路线图"时，创建简化的路线可视化：

### 路线图包含要素
1. **总览表**：日期、星期、起点（前一晚酒店）、终点（当晚酒店）、主要路线、驾驶距离、驾驶时间、核心景点
2. **每日详细路线**：从前一晚住宿开始，到当晚住宿结束的完整路线
3. **路线流程图**：用ASCII箭头表示每天行程（起点→景点1→景点2→终点）
4. **空间路线概览**：整个行程的地理布局示意图（南疆段、北疆段分开画）
5. **租车信息**：分段租车取还车地点、时间、车型
6. **路线优化提示**：无重复路说明、加油盲区提醒、住宿提前订提醒

### 路线图格式
- 用Markdown表格展示总览
- 每日路线用ASCII流程图（`↓`箭头连接各点）
- 标注驾驶距离和时间（`↓ 约XXXkm / Xh`）
- 关键节点标注导航关键词（`📍导航关键词`）
- 重要提醒用⚠️标识（加油盲区、高海拔、无信号等）

### 与详细攻略的关系
- 路线图是详细攻略的**简化可视化版本**，不重复景点信息
- 详细攻略包含所有景点信息（门票、玩法、注意事项）
- 路线图只保留路线骨架和关键提醒
- 用户可以在路线图和详细攻略间快速切换参考

### 输出格式选择（根据用户需求）
1. **Markdown版本**（默认）：ASCII路线图 + 表格，适合聊天窗口直接显示
2. **HTML交互版本**：用户说"搞个HTML版本"或"可视化地图"时，创建基于Leaflet的交互式地图
3. **ASCII纯文本版本**：用于聊天窗口直接显示（当MEDIA附件发送失败时，直接输出内容）

## HTML交互式地图创建

当用户要求"HTML版本"或"可视化地图"时，创建交互式HTML地图：

### 技术选型
- **地图库**：Leaflet.js（轻量、开源、易用）
- **地图瓦片**：**必须使用国内可访问的地图服务**，OpenStreetMap在国内被墙
- **推荐瓦片源**：
  - **高德地图**（首选）：需要API key，获取方式见下方
  - 腾讯地图（备选）：需要申请key
  - 天地图（备选）：需要申请key（tk参数）

### 高德地图API key获取方法
1. **从现有项目中查找**：检查用户的其他项目（如旅行项目），在`src/components/`或配置文件中搜索`AMAP_KEY`或`amap`关键字
2. **代码中常见位置**：
   - `const AMAP_KEY = '...'`（如`FootprintMap.tsx`）
   - 环境变量文件（`.env.local`、`.env.production`）
   - 配置文件（`config.js`、`config.ts`）
3. **搜索命令**：
   ```bash
   grep -r "amap\|AMAP\|11f75d76b215d1706a5309de5eac85c1" /home/ubuntu/
   ```

### 高德地图Leaflet配置（带API key）
```javascript
// 高德地图瓦片（使用API key）
const AMAP_KEY = '从项目获取的key';
L.tileLayer(`https://webrd0{s}.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8&x={x}&y={y}&z={z}&key=${AMAP_KEY}`, {
    subdomains: '1234',
    attribution: '© 高德地图',
    maxZoom: 18
}).addTo(map);
```

### 高德地图Leaflet配置（备选：天地图，不需要key）
```javascript
// 天地图瓦片（不需要key，但需要tk参数）
L.tileLayer('http://t{s}.tianditu.gov.cn/DataServer?T=vec_w&x={x}&y={y}&l={z}&tk=1234567890', {
    subdomains: ['0', '1', '2', '3', '4', '5', '6', '7'],
    attribution: '© 天地图',
    maxZoom: 18
}).addTo(map);
// 添加注记层（显示地名）
L.tileLayer('http://t{s}.tianditu.gov.cn/DataServer?T=cva_w&x={x}&y={y}&l={z}&tk=1234567890', {
    subdomains: ['0', '1', '2', '3', '4', '5', '6', '7'],
    attribution: '',
    maxZoom: 18,
    opacity: 0.7
}).addTo(map);
```

### HTML地图功能要求
1. **地图显示**：显示完整路线（不同颜色区分不同路段）
2. **城市标记**：用圆形标记，点击显示城市名称
3. **路线绘制**：用Polyline连接各点，不同颜色区分
4. **Tab切换**：每天一个Tab，点击显示当天详细路线信息
5. **响应式设计**：支持手机和电脑访问
6. **图例说明**：显示路线颜色含义

### HTML地图结构
```html
<div class="header">标题和副标题</div>
<div id="map"></div>
<div class="legend">路线说明图例</div>
<div class="tabs">每日Tab按钮</div>
<div id="day1" class="day-content">Day1详细信息</div>
<!-- 其他天... -->
<div class="footer">底部说明</div>
```

### 文件交付注意
- QQ平台：优先尝试MEDIA:路径发送文件，失败时直接在聊天窗口输出内容
- 其他平台：根据平台特性选择合适交付方式

**参考示例**：见 `references/xinjiang-route-map-example.md`（新疆9天自驾路线图示例）
**HTML模板**：见 `templates/route-map-template.html`（交互式地图HTML模板）
**API key查找**：见 `references/amap-key-retrieval.md`（从现有项目中查找高德地图API key）
**彩色路线图**：见 `references/multi-day-color-coded-map.md`（多日行程彩色路线图模式）

## 多日行程彩色路线图

当用户要求在总览地图上区分每天路线时，使用**不同颜色的线段**表示每一天：

### 颜色方案
| 日期 | 颜色代码 | 场景 |
|------|----------|------|
| Day1 | #FF6B6B | 抵达日 |
| Day2 | #4ECDC4 | 高原日 |
| Day3 | #45B7D1 | 返程日 |
| Day4 | #96CEB4 | 长途日 |
| Day5 | #FFEAA7 | 多景点日 |
| Day6 | #DDA0DD | 草原日 |
| Day7 | #FFB347 | 公路旅行日 |
| Day8 | #87CEEB | 风景日 |
| Day9 | #98D8C8 | 返程日 |

### 实现要点
1. **总览与详情分离**：总览用彩色区分日期，详情用统一颜色显示当天路线
2. **标记颜色一致**：标记点使用与当天路线相同的颜色
3. **浅色背景文字**：Day4/Day5颜色较浅，标签文字用深色(#333)
4. **图例分层**：上方每日颜色，下方地点类型（酒店/景点/餐饮）

**详细实现**：见 `references/multi-day-color-coded-map.md`

### HTML地图内容结构

每个Day的内容区块应包含完整的时间线：

```html
<div id="day1" class="day-content">
    <h2>Day 1 (6/2 周二)：标题</h2>
    <div class="timeline">
        <div class="timeline-item">
            <div class="timeline-time">16:10-16:30</div>
            <div class="timeline-content">📍 具体安排</div>
        </div>
        <!-- 更多时间段 -->
    </div>
    <div class="tip">💡 提示信息</div>
    <div class="warning">⚠️ 住宿/注意事项</div>
</div>
```

**关键**：day-content必须包含攻略文档中的**完整时间线信息**，不要只写简略路线。用户可以直接在页面上查看当天全部安排，无需来回切换。

## 服务器部署流程

当用户要求部署HTML地图到服务器时：

### 部署步骤
1. **创建项目目录**
   ```bash
   mkdir -p /home/ubuntu/{项目名}
   cp {HTML文件} /home/ubuntu/{项目名}/index.html
   ```

2. **启动HTTP服务器**
   ```bash
   # 使用Python内置HTTP服务器
   cd /home/ubuntu/{项目名}
   python3 -m http.server {端口} --bind 0.0.0.0 &
   ```

3. **检查服务器状态**
   ```bash
   netstat -tlnp | grep ":{端口}"
   ```

4. **验证访问**
   ```bash
   curl -s -o /dev/null -w "%{http_code}" http://localhost:{端口}/
   ```

### 端口选择建议
- 常用端口：8080、8888、9000
- 检查端口占用：`netstat -tlnp | grep ":{端口}"`
- 避免使用已被占用的端口（如3000、3001、8899等）

### GitHub仓库创建
```bash
# 1. 初始化Git仓库
cd /home/ubuntu/{项目名}
git init
git add .
git commit -m "Initial commit"

# 2. 创建GitHub仓库（使用GitHub API）
curl -H "Authorization: token {TOKEN}" -d '{"name":"{仓库名}","description":"{描述}","private":false}' https://api.github.com/user/repos

# 3. 添加远程仓库并推送
git remote add origin https://github.com/{用户}/{仓库名}.git
git push -u origin master
```

### 安全组配置提醒
- 腾讯云服务器需要在安全组中开放相应端口
- 默认可能只开放80、443端口
- 如需访问自定义端口（如8080），需在安全组中添加入站规则

### 访问地址格式
- 内网访问：`http://localhost:{端口}`
- 外网访问：`http://{服务器IP}:{端口}`（如：`http://43.139.81.132:8080`）

## 地图导航链接处理

### 高德地图导航链接格式
```javascript
// 正确格式：使用真实地名，不是显示名
const amapUrl = `https://uri.amap.com/marker?position={经度},{纬度}&name={真实地名}&coordinate=gaode&callnative=1`;
```

### Marker数据结构设计
每个marker需要两个名称：
- `name`：显示名（用于地图标签和弹窗标题，可带emoji和day标记）
- `navName`：导航名（用于高德地图搜索，必须是真实地名）

```javascript
// 示例
{name: 'Day5 🇨🇳 霍尔果斯', coord: [44.12, 80.42], type: 'day5', navName: '霍尔果斯国门景区'}
```

### 弹窗内容设计
```javascript
const popupContent = `
    <div style="text-align:center;">
        <b>${marker.name}</b><br>           <!-- 显示名 -->
        <span style="color:#999;font-size:11px;">${navName}</span><br>  <!-- 真实地名 -->
        <a href="${amapUrl}">📍 高德地图导航</a>
    </div>
`;
```

### 关键原则
1. **navName必须是高德地图能搜到的真实地名**，如"霍尔果斯国门景区"而非"Day5 🇨🇳 霍尔果斯"
2. **弹窗同时显示显示名和真实地名**，方便用户在其他设备上手动搜索
3. **经度在前，纬度在后**：高德URI格式是 `position={lng},{lat}`

---

## 坐标校验

### 常见坐标错误
地图坐标可能来自网络，存在偏差。校验方法：
1. 以已知准确坐标为参考点（如城市中心），计算相对位置
2. 特克斯县城 [43.21, 81.84] 是准确参考点
3. 喀拉峻景区应在特克斯东南20-30km，坐标约 [43.12-43.18, 81.88-81.95]
4. 如果景区坐标经度比城市大0.3度以上，很可能是错的（0.3度≈25km）

### 坐标系注意
- 高德地图使用GCJ-02坐标系（火星坐标）
- 如果使用GPS坐标（WGS-84），需要转换后才能准确定位
- 新疆地区坐标偏差影响相对较小，但仍需注意

---

## 常见陷阱

1. **信息割裂**（最严重！）：把景点介绍、门票、玩法分开写在不同section→用户需要来回翻→体验极差。用户原话："信息有一点点割裂"。解决方法：所有信息嵌入时间线对应位置，像用户发来的参考攻略图片那样写。
2. **只写景点名不解释**：用户可能不知道"唐布拉百里画廊"是草原/公路/瀑布→必须用大白话说清楚"它是什么"
3. **导航链接用显示名**：marker显示名带emoji和day标记（如"Day5 🇨🇳 霍尔果斯"），无法在高德地图搜索。必须用navName字段存真实地名
4. **坐标不准确**：网上查的坐标可能偏差20-30km，尤其是景区内的具体景点。以附近已知城市为参考点校验
5. **时间过紧**：没考虑停留拍照、排队、吃饭、找路的时间
6. **忽略人员组成**：年轻人和长辈的耐受度完全不同
7. **景点重复体验**：同一趟行程不需要两个类似景点（如两个观光草原→砍一个换不同类型）
8. **不考虑当地时差/作息**：如新疆实际晚2小时，按北京时间9点出发=当地7点，太早了
9. **赶路日连续安排**：连续两天6h+车程会让人崩溃，尽量把辛苦日拆分到隔天
10. **晚归+早起冲突**：如23:00回酒店但第二天要06:30出发=只睡6h，对长辈不友好
11. **国内地图服务不可访问**：OpenStreetMap在国内被墙，必须使用高德地图、腾讯地图等国内服务。用户报告"地图看不了"时，首先检查是否使用了国外地图服务
12. **文件发送问题**：QQ平台MEDIA附件发送可能失败，需要直接在聊天窗口输出内容作为备选方案

---

## 技能更新与维护

当用户要求更新此技能时，需要合并GitHub版本和本地版本的功能。详见 `references/skill-update-merging.md`（技能版本合并经验）。

### 合并检查清单
- [ ] 保留新版本的信息收集表格和触发条件
- [ ] 合并旧版本的HTML地图、部署、路线审查等高级功能
- [ ] 更新版本号（如v2→v3）
- [ ] 确保references和templates目录完整
- [ ] 推送更新到GitHub仓库